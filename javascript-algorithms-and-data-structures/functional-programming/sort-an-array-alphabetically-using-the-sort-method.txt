function alphabeticalOrder(arr) {
  // Only change code below this line
  console.log(arr.sort())
  

  return arr.sort()
  // Only change code above this line
}
alphabeticalOrder(["a", "d", "c", "a", "z", "g"]);
alphabeticalOrder([9,3,2,1]);